import 'package:flutter/material.dart';

class PillScheduleScreen extends StatelessWidget {
  const PillScheduleScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pill Schedule Screen'),
    );
  }
}
